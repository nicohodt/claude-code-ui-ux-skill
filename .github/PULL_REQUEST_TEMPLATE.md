<!--
Thanks for opening a PR — genuinely. 🎉

Two sentences is enough. Don't worry about getting the checklist perfect:
if something's missing we'll tell you exactly what to change, or push the fix ourselves.
Nothing here is a reason for us to close your PR.
-->

## What does this PR change?

<!-- One or two sentences. -->

## Why?

<!-- Closes #123, or describe the problem it solves. -->

---

### Checklist

<!-- Tick what applies. Unticked boxes are fine — they're a prompt, not a gate. -->

- [ ] Changes were made in `src/ui-ux-pro-max/` (the source of truth), not directly in `cli/assets/` or `.claude/`
- [ ] Ran `npm run sync:assets && npm run check:assets` in `cli/` (only needed if you touched `data/`, `scripts/` or `templates/`)
- [ ] Ran `npm run validate:csv` (only if you edited a CSV)
- [ ] Commit messages use [Conventional Commits](https://www.conventionalcommits.org/) (`feat:`, `fix:`, `docs:` …)

### First time contributing here?

Say so below and we'll walk you through anything that's unclear — including running the sync step for you. First PRs are welcome and expected.
